<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
<head>
    <!-- ...existing code... -->
    <meta name="csrf-token" content="{{ csrf_token() }}">
    <!-- ...existing code... -->
</head>
<body>
    <!-- ...existing code... -->
</body>
</html>